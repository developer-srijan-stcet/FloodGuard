{
  "name": "flash-flood-predictor",
  "private": true,
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "frontend:install": "npm --prefix frontend install",
    "frontend:lint": "npm --prefix frontend run lint"
  }
}
